# 百万字长篇小说编辑器

这是一个零依赖的前端原型：
- 输入简单背景情节。
- 自动拆分百万字级别的卷级大纲与章节规模。
- 按批次持续生成章节草稿。
- 实时统计完成度并导出 Markdown。

## 使用方式

直接在浏览器打开 `index.html`，或在本地起一个静态服务：

```bash
python3 -m http.server 4173
```

然后访问 `http://localhost:4173`。
